@extends('layouts.app')

@section('content')
<div class="container-fluid">
    {{$context}}
    {{-- @if(count($venues\) > 0)

    @else
        No venue for this event
    @endif --}}
</div>
@endsection
